package dao;

import clases.Rol;
import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.util.ArrayList;
import java.util.List;

public class MySqlRolDAO {

    public boolean registrar(Rol rol) {
        // Código para insertar un rol
        return false;
    }

    public boolean actualizar(Rol rol) {
        // Código para actualizar un rol
        return false;
    }

    public boolean eliminar(int id) {
        // Código para eliminar un rol
        return false;
    }

    public Rol buscar(int id) {
        // Código para buscar un rol
        return null;
    }

    public List<Rol> listar() {
        // Código para listar los roles
        return new ArrayList<>();
    }
}
